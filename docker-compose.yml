version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: glanus
      POSTGRES_PASSWORD: glanus
      POSTGRES_DB: glanus
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: always

volumes:
  pgdata:
